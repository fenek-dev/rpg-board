import { RenderPopups } from '~/widgets/popups/ui/RenderPopups';
import { Menu } from '~/widgets/settings/ui/Menu';

function App() {
  return (
    <main>
      <Menu />
      <RenderPopups />
    </main>
  );
}

export default App;
